# The community is intentionally hidden before the public launch campaign.
# Set this to true only when the community is ready to be announced.
NEXT_PUBLIC_COMMUNITY_ENABLED=false

# Load the built-in editorial archive, exhibition and discussion entries.
# Keep false on the formal site; enable only in an isolated review environment.
SEED_EDITORIAL_CONTENT=false
